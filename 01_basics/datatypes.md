<!-- Objects Types / Data Types -->

<!-- -Numeric : 1234 , 3.14 , 3+4j , Decimal , Fraction

-String : 'spam' , "guido's" , b'a\x01c' , u'sp\xc4m'

-List : [1, [2, 'three'], 4.5], list(range(10)) // Continuous memory space allocate krti ap ko jo ka index k through access krwati hain

-Tuple : (1, 'spam', 4, 'U') , tuple('spam') // Read only list
-Dictionary : {'food': 'spam', 'taste': 'yum'} , dict(hours=10) // Key value pair

-Set : set('abc') , {'a', 'b', 'c'} // Unique values

-Frozen Set : frozenset('abc') // Read only set

-Bytes : b'spam' , bytes([1, 2, 3, 4]) // Raw byte values
-Boolean : True , False
-Function : def foo() : pass // Function
-Module : import sys // Module
-Classes : class Spam : pass // Class
-File : open('eggs.txt') , open(r'C:\ham.bin', 'wb') // File object
-Advance Types : Iterators , Generators , Decorators , Context Managers , Meta-Progarmming

-None : None // Null value

Python suppprt high level data types allowing you to focus on the solution rather than the syntax.You can import libraries ,functions :  Math(), Random()  -->